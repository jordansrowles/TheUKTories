@page
@model TheUKTories.FrontendApp.Pages.Portal.UK.Covid.Responses.Sources.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CovidGovResponseSource[0].CovidGovResponse)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CovidGovResponseSource[0].Source)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CovidGovResponseSource[0].Link)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CovidGovResponseSource[0].Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CovidGovResponseSource) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CovidGovResponse.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Source)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Link)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CovidGovResponseSourceId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CovidGovResponseSourceId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CovidGovResponseSourceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
