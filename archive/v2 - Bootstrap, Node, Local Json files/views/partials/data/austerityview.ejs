<ul id="austerity-list" class="separated-list">
</ul>
<script>
$.getJSON("data/austerity.json", function(json) {
var json = JSON.parse(JSON.stringify(json));
var list = document.getElementById('austerity-list');

for (var item = 0; item < json.items.length; item++) {
    var obj = json.items[item];
    var li = document.createElement('li');
    li.innerText = obj.string;

    var sm = document.createElement('small');
    sm.innerText = ' - '

    for (var s = 0; s < json.items[item].sources.length; s++) {
    var source = obj.sources[s];
    var link = document.createElement('a');
    link.setAttribute('href', source.link);
    link.setAttribute('target', '_blank');
    link.innerText = source.source;
    sm.append(link);
    li.append(sm);
    }

    list.append(li);
}

});
</script>